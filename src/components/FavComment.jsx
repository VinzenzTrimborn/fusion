// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {PlasmicFavComment} from "./plasmic/fusion_lab/PlasmicFavComment";

function FavComment_(props, ref) {
    let {comment= {}} = props;
    const {likes, text, username} = comment;
    return <PlasmicFavComment
        text={text}
        username={username}
        likes={likes}
        root={{ref}}
        {...props}
    />;
}

const FavComment = React.forwardRef(FavComment_);

export default FavComment;
